#!/usr/bin/env node
// substrate-hook.js — back-compat stub for the Spor rename (SPLIT.md):
// forwards to spor-hook.js, whose main() runs on require. Remove after the
// dual-read window closes.
require("./spor-hook.js");
